import SceneClip from '@/components/explore/SceneClip'

/** 幕正文：q-tiered-execution（yaml label：第三层：分级执行）—— 列表动画 + 决策流程图 */
export default function QTieredExecution() {
  return (
    <>
      <SceneClip />
      <p>在平台内部，按照协议仓库里声明的接口等级，执行不同的约束策略：</p>
      <ul>
        <li><strong>只读接口</strong>：直接调用，不需要任何申请；</li>
        <li><strong>安全写接口</strong>：弹确认卡，获得本人确认后立即直打生产；</li>
        <li>
          <strong>写接口（预演级）</strong>：真正动生产之前，先以同一份参数打一次<strong>测试服</strong>，
          预演结果直接展示在确认卡里——人看到的是"这个操作会产生什么效果"，而不是一堆抽象参数。
          确认无误后点「生效」，同一份参数才打到生产服。这一步解决的是行为结果的可观测性；
        </li>
        <li><strong>可逆接口的撤回</strong>：可逆是一个独立的标记，和等级正交。事后如果发现出了问题，在调用历史里点一下"撤回"即可回滚操作。</li>
      </ul>
      <p>确认卡本身也有几道安全约束：确认记录落库、有过期时间、<strong>只有发起人本人能点确认或拒绝</strong>（身份在服务端校验，别人拿到链接也没用）。</p>
      <p>还有一个不起眼但关键的约束：<strong>确认通过的就是被执行的那份参数</strong>。对话路径下参数存在会话快照里，直调路径下存在确认记录里，恢复执行时原样取出——不存在"审批的是 A、执行的是 B"的可能。这在代码层面实现，并不难，但缺了它，前面所有分级确认都会形同虚设。</p>
      <p>把这套策略画成决策流程图（列表动画播完后淡入），读法分上下两段。上半段是执行前：AI 发起调用后先判断接口属于哪一级——只读直调生产；安全写经本人确认后直打生产；写级先送测试服预演，本人在界面上看到预演结果后点「生效」，同一份参数才上生产。下半段是执行后：事后发现问题时，可逆操作一键撤回；不可撤回的进复盘会，回放决策链定责。</p>
      <p>这套逻辑清晰明了，代码量也不大，更多的工作量其实在 UI 设计上。核心链路用足够的单元测试保护起来即可。</p>
      <p>到这里，四个前提全部闭环：权限沿用 RBAC（前提 1）、分级确认 + 预演 + 可撤回 + 载荷锁定（前提 2、3）、核心逻辑精简（前提 4）。</p>
    </>
  )
}
