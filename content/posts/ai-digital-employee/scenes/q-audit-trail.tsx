import SceneClip from '@/components/explore/SceneClip'

/** 幕正文：q-audit-trail（yaml label：操作留痕：每次调用可查，写错可撤） */
export default function QAuditTrail() {
  return (
    <>
      <SceneClip />
      <p>平台上每一次工具调用都会留下一条记录：谁调的、什么时候、调了哪个工具、传了什么参数、结果如何。网页上有一个操作历史页，普通用户只能看自己的记录，管理员可以按人切换查看。</p>
      <p>光留痕还不够。声明了「可逆」的写操作，可以从历史里直接撤回——撤回不是删记录，而是触发这个工具对应的逆向操作，页面上这一行随后标记为已撤回。出过什么事、后来怎么处理的，都查得到。</p>
      <p>撤回能成立的前提，是工具本身提供了逆向操作。所以「可逆」是协议仓库里需要认真声明的属性，不是平台自动变出来的。</p>
    </>
  )
}
