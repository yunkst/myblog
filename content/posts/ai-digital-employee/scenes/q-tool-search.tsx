import SceneClip from '@/components/explore/SceneClip'

/** 幕正文：q-tool-search（yaml label：工具按需检索：命中才进模型工具栏） */
export default function QToolSearch() {
  return (
    <>
      <SceneClip />
      <p>协议仓库里的接口会越来越多。如果把几百个工具的描述全部塞进模型上下文，一是调用成本高，二是模型的注意力被稀释，三是敏感工具平白暴露在模型面前。</p>
      <p>所以工具默认<strong>不</strong>进上下文。模型常驻的只有两个基础工具：一个用来向用户追问，一个用来检索工具。用户提出具体问题后，检索步骤拿着问题在候选池里挑出最相关的 1~5 个，注入模型的工具栏，下一轮模型就能像调用普通工具一样调用它们。</p>
      <p>两个细节。一是检索只看工具名和描述，不展开参数细节，这一步本身很省。二是命中注入之后，等级分流照常生效——只读直接跑，写操作依然要过人确认，不会因为走了检索通道就跳过任何一级约束。</p>
    </>
  )
}
