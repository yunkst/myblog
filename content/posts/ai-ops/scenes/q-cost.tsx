import SceneClip from '@/components/explore/SceneClip'

/** 幕正文：q-cost（yaml label：节约成本：稳定性分级） */
export default function QCost() {
  return (
    <>
      <SceneClip />
      <p>
        这套体系跑在「稳定性分级」策略上。核心依据：服务的稳定性需求越高，投入越多，但边际效益递减——
        从 99% 到 99.9% 的成本，可能是 99.9% 到 99.99% 的十分之一。所以与其全部拉高，不如分级。
      </p>
      <p><strong>节点策略：包年保底 + 竞价冲量</strong></p>
      <ul>
        <li>腾讯云竞价实例价格约为包年包月的 1/8，缺点是可能被回收；</li>
        <li>租 2 个包年包月节点，通过亲和性把关键服务钉在上面——APISIX、ArgoCD（及其 etcd）这类「挂了整个体系瘫痪」的组件；</li>
        <li>其余服务（测试环境、内部后台、可中断的批处理）全部落竞价节点；</li>
        <li>极端情况：竞价节点同时被回收，关键链路仍然存活，损失的只是测试和后台服务。</li>
      </ul>
      <p><strong>优先级腾挪</strong>：用 PriorityClass 表达三级水位——生产对外服务 &gt; 内部后台 &gt; 测试。
        生产服务配 HPA（副本 ≥2 的 Deployment 强制配）；内部后台（巡检、监控、CI）低于生产、高于测试；
        测试单副本起步——反正每晚会被数据分身覆盖。配套规范：每个 Deployment 必须配 VPA（建议模式），
        用实际运行数据回答「这个服务到底需要多少资源」，避免拍脑袋超配。
      </p>
      <p>
        <strong>2 倍容量、1/3 成本</strong>：这两个数能同时成立，靠的是组合——2 倍容量里只有约 1/4
        （2 个包年节点）按正价付费，其余全是竞价价格。容量冗余换的是竞价回收时的缓冲空间。
      </p>
      <p><strong>再往上，稳定性怎么加</strong>（成本从低到高四档）：</p>
      <ol>
        <li><strong>消除组件单点</strong>（最便宜一档）：Infisical 的 PG、ArgoCD、GitLab 等单副本组件切多副本 + 外部数据库——大部分「集群挂了」的事故其实是某个单点组件挂了；</li>
        <li><strong>备份异地化</strong>：COS 里的数据库备份和 Git 仓库同步一份到跨区域/跨云对象存储。GitOps 体系有天然优势——整个集群的声明状态就是那个 Git 仓库，仓库在，集群就能重建；</li>
        <li><strong>多集群热备</strong>：第二个集群跑同样的 GitOps bootstrap（App of Apps 下换个 destination 重新 apply），数据靠备份恢复。RTO 小时级、RPO 备份间隔，对多数非金融业务够用；</li>
        <li><strong>多云 / 异地双活</strong>：要解决跨云网络、数据同步、全局流量调度三个问题，复杂度和成本陡增，除非 RTO/RPO 要求到分钟级否则不建议一步到位。</li>
      </ol>
      <p>
        实际选择停在第 2 档：单集群 + 关键组件消除单点 + 备份异地化。
        这套体系服务的是可以容忍小时级重建的业务，为用不到的稳定性付月费不值。
      </p>
    </>
  )
}
